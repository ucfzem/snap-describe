import React from 'react'
import ReactDOM from 'react-dom/client'
import ImageCaptionApp from './ImageCaptionApp'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ImageCaptionApp />
  </React.StrictMode>,
)
